Django==4.2.7
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
channels==4.0.0
daphne==4.0.0
python-dotenv==1.0.0
numpy==1.24.3
Pillow==10.0.1
APScheduler==3.10.4
fpdf2>=2.7.0

# ============================================================
# ML / ranking libraries
# ============================================================
# Priority ranking  -> interpretable weighted-sum formula (pure Python, no dep)
# Deadline risk     -> engagement heuristic (pure Python, no dep)
# Smart search      -> token + synonym + fuzzy match (pure Python, no dep)
# Recommendations   -> Truncated SVD matrix factorisation
scikit-learn>=1.3.0

# NOTE: torch / transformers / sentence-transformers / lightgbm / lifelines
# were removed – they added ~2 GB, needed model downloads, slowed every
# request and crashed on some Windows setups (torch DLL load error).
